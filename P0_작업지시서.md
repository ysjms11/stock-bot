# P0 작업지시서

변경 9개를 main.py에 적용:

1. PORTFOLIO_FILE 아래에 WATCHALERT_FILE = "/data/watchalert.json" 추가

2. _is_us_ticker() 아래에 _NYSE_TICKERS 세트 + _guess_excd(symbol) 함수 추가 (NYS/NAS 판별)

3. load_dart_seen() 아래에 load_watchalert() 추가

4. kis_us_stock_price의 excd 기본값 "NAS"→"" 변경, if not excd: excd = _guess_excd(symbol) 추가

5. kis_us_stock_price 아래에 kis_us_stock_detail 함수 추가 (경로: /uapi/overseas-price/v1/quotations/price-detail, TR_ID: HHDFS76200200, 필드: perx/pbrx/epsx/tomv/h52p/l52p/e_icod)

6. _execute_tool get_portfolio 미국 섹션에서 d.get("diff_rate") → d.get("rate") 수정 (chg_today null 버그)

7. MCP_TOOLS에 4개 추가: get_us_stock_detail, set_watch_alert, get_watch_alerts, remove_watch_alert

8. _execute_tool에 4개 핸들러 추가

9. check_stoploss에 watchalert 매수감시 체크 통합 (현재가 <= buy_price이면 텔레그램 🟢 알림)

커밋메시지: "add CLAUDE.md and P0 task spec"
