# TICKET ② — US 스테일 재발방지 + WS 재연결 루프 진단

> 2026-07-21 | 배포: 가격캐시 TTL(커밋 직후) | 재연결 하드닝: 별도 티켓 제안

## A. 배포됨 — WS 가격캐시 TTL (correctness, ②의 핵심)
- **문제**: `ws_manager.get_cached_price()`가 TTL 없이 옛 가격 무기한 서빙 → WS 틱 유실 시 stoploss·portfolio·telegram·MCP·대시보드가 스테일가 사용. 실사건: XNDU 저장가 11.75가 10거래일 동결(실가 $9.99), CRITICAL -15.8%가 왜곡 평가액 기반.
- **픽스**: `_price_cache`를 `(price, time.time())` 튜플화 + `get_cached_price`에 180초 TTL 게이팅. 초과 시 None 반환 → 소비자 9곳 전부 기존 `if cached is None: REST/Yahoo` fallback으로 자동 신선조회. **소비자 무수정.**
- **게이트**: dev(918 passed·테스트 12종·XNDU 재현) → Opus 리뷰 PASS(blocker 0, 소비자 9곳·튜플완전성·REST무폭주·알림무누출 검증) → 재시작·라이브확인(TTL 상수 로드).
- **TTL 180초 근거**: stoploss 주기 600초 < TTL이면 무의미, > TTL이면 틱 단절 시 매 사이클 신선조회. 40종목 동시 만료해도 stoploss 0.3s 직렬화(~3.3/s < KIS 10/s 한도). 잠정값 — 튜닝 가능.

## B. 진단만 — WS 재연결 루프 (별도 티켓, 라이브 생명주기라 미구현)
읽기전용 조사 + 코드/로그 실측 (이전 세션 발언 2건 정정 포함):

**정정**: (1) "9443 오류 현재진행"은 오류 — 마지막 9443/접속키오류 = 로그 라인 415846(7/6 이전 과거장애), 최근 없음. (2) "9,082회/2주"는 부정확 — "연결됨" 9,152는 **로그 전체**(봇 재시작 42회 포함), 최근 15.5일 실재연결 ≈ 2,802.

**근본원인**: KIS 서버가 **휴장 시간대 유휴세션을 ~90초에 조용히 종료**(close code 미로깅) + 클라 `_run_loop`의 **고정 30초 재시도(백오프·휴장게이트 없음)**. 폭주의 ~89%가 주말/공휴일(토 392·일 632·월새벽 218/주말, 제헌절 7/17 216회). **US 실장중엔 안정 → 재연결은 US 야간 틱유실의 주범 아님**(주범=무TTL 캐시, A에서 조치).

**가설 판정**: update_tickers 재연결(기각, 델타구독만)·구독한도41(기각, 40캡)·접속키만료(기각, 최근 0)·**KIS 휴장 유휴세션 종료(채택)**. 공범 후보: `_on_text`가 `{` JSON 전면무시 → **KIS 앱레벨 PINGPONG 무응답**(공식샘플은 pong echo 필수)이 서버 킥의 근거일 수 있음(미확정, 주말 관측 필요).

**코드 발견**: `_run_loop:129` `now=datetime.now(KST)` 데드변수 / `_connect_and_run:166-171` CLOSED·ERROR 분기가 데드코드(aiohttp async-for가 서버 CLOSE 시 조용히 종료 → "연결 종료됨" 출력 0회, 그래서 원인 은폐) / `_on_text:185` JSON 전면무시로 구독거절 ACK·PINGPONG 침묵.

**하드닝 권고 (별도 티켓, 우선순위 낮음 — 실질 비용=KIS 요청스팸·로그오염, correctness 아님)**:
- 🟢 안전: ① 종료 경로에 close code·세션수명 로깅(데드코드 분기 정리) ② KR+US 동시 휴장 시 재연결 간격 확대(휴장 게이트) ③ 연속 즉사 시 지수 백오프.
- 🟡 실험: ④ KIS PINGPONG echo 응답 — ~90초 킥의 서버측 원인 가설, 구현 후 주말 관측으로 검증.
- ⚠️ 라이브 WS 생명주기(`_run_loop`/`_connect_and_run`) 변경이라 TTL과 분리, 운영자 승인 후.

---

## C. 2026-08-16 추가: 8/7~8/14 침묵장애 사후 재발방지 배포 (커밋 8dbf980)
- **사건**: 봇 프로세스 아웃바운드만 8일 사망(수집 30연속 실패·DART 0건·스냅샷 동결·텔레그램 발송 전멸), 인바운드 생존으로 무증상. 감시견은 같은 네트워크라 보고 불능(구조적 맹점). /tmp 로그가 8/15 재부팅에 소실돼 트리거 미상. 재부팅이 치료.
- **복구**: 8/7 시장데이터 수동백필(600종목), 8/10~14는 sanity 자동백필 (윈도우 확대로 커버).
- **재발방지 배포**: ① 로그 영속화(plist /tmp→~/Library/Logs, 트림잡 동기) — 포렌식 생존 ② /health에 last_snapshot·last_collect 노출(300s캐시·실패시 30s·RO연결) — "인바운드만 생존" 상태를 외부에서 감지 가능 ③ sanity 윈도우 5→10영업일+SQL컷오프 25일+임계 >=500+백필 5일/회 캡+1800s 타임아웃 (리뷰 중요3 반영, 오탐루프 차단).
- **미구현(설계만)**: 아웃바운드 연속실패 시 프로세스 자가재시작 — 재시작 루프 리스크로 운영자 승인 필요. 외부 모니터(UptimeRobot류)에 /health JSON 신선도 판정 연결도 운영자 몫.
- 배포 중 bootstrap EIO 1회(bootout 직후 레이스) — 재시도로 해소, 다운타임 ~40초.
