python-telegram-bot[job-queue]>=21.10,<22.0  # 5/9 PTB v20+ days= 매핑 변경 사고 후 메이저 핀 (학습 #31)
aiohttp>=3.10.0
tzdata
beautifulsoup4
lxml
requests
finance-datareader>=0.9.50
yfinance>=0.2.0
pdfplumber>=0.10.0
numpy>=1.24.0
scipy>=1.10.0
pykrx>=1.2.8  # 5/8 KRX 응답 구조 변경으로 1.2.4 호환 깨짐 → 1.2.8 minimum
pyarrow>=14.0.0  # parquet 엔진 — div_yield 재구성 sidecar(auto-stock/RALPH/divyield_reconstruct) + probe 파이프라인. 누락 시 to_parquet/read_parquet 침묵 실패
python-dotenv>=1.0.0
youtube-transcript-api>=1.2.0
# ── 테스트 (런타임 무관, 다른 환경 셋업 시 누락하면 async 테스트 ~82개 즉시 실패) ──
pytest>=8.0
pytest-asyncio>=1.4  # pytest.ini 의 asyncio_mode=auto 와 함께 동작 (2026-06-04 도입)
